/**
 * Used for production builds instead of environment.ts — see the
 * fileReplacements entry in angular.json.
 *
 * Set the deployed API address here before running `npm run build`.
 */
export const environment = {
  production: true,
  apiUrl: 'https://REPLACE-WITH-YOUR-API.onrender.com/api',
};
