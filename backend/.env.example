# Copy this file to .env and adjust if needed.
DATABASE_URL="file:./dev.db"
# Port 3000 is a popular default and is often taken by other software
# (VPN clients, Docker, other dev servers), so the API sits on 3333.
PORT=3333
CORS_ORIGIN="http://localhost:4200"
